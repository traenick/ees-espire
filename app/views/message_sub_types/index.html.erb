<%- model_class = MessageSubType -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th><%= model_class.human_attribute_name(:message_type_id) %></th>
      <th><%= model_class.human_attribute_name(:sub_type_name) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @message_sub_types.each do |message_sub_type| %>
      <tr>
        <td><%= link_to message_sub_type.id, message_sub_type_path(message_sub_type) %></td>
        <td><%= message_sub_type.message_type.name %></td>
        <td><%= message_sub_type.sub_type_name %></td>
        <td><%=l message_sub_type.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_message_sub_type_path(message_sub_type), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      message_sub_type_path(message_sub_type),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_message_sub_type_path,
            :class => 'btn btn-primary' %>
