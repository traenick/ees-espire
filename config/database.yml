# MySQL.  Versions 4.1 and 5.0 are recommended.
# 
# Install the MYSQL driver
#   gem install mysql2
#
# Ensure the MySQL gem is defined in your Gemfile
#   gem 'mysql2'
#
# And be sure to use new-style password hashing:
#   http://dev.mysql.com/doc/refman/5.0/en/old-client.html
development:
  adapter: postgresql
  database: ees_rails_app
  host: localhost
  username: ees_rails_app
  pool: 5
  timeout: 5000


  #adapter: mysql2
  #encoding: utf8
  #reconnect: false
  #database: ees_acre_development
  #pool: 5
  #username: root
  #password:
  #socket: /tmp/mysql.sock


production:
  adapter: postgresql
  database: ees_rails_app
  host: localhost
  username: ees_rails_app
  pool: 5
  timeout: 5000

